import Swal from 'sweetalert2/dist/sweetalert2.js';

import withReactContent from 'sweetalert2-react-content'

const MUISwal = withReactContent(Swal)

window.MUISwal = MUISwal;

// window.Swal = Swal;